
/* invalid(); /* */ 
// invalid();
int encloseParams(FILE* paramsFileptr) {
  hello() + world() + lmao() + wow()
  valid()
  listFunctions()
  // this shouldn't break anything } int invalid(); }
}
void valid();


int listFunctions(FILE* codeFile, char res[BUFSIZE]) {
  listFunctions()
}


int main() {
  listFunctions(a)
}
